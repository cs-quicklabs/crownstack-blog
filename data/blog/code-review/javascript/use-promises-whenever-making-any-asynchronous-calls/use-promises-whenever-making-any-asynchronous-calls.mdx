# Use promises whenever making any asynchronous calls

In order to avoid the race conditions the the promises must be use. Using promises we can apply async await coupled with resolve or reject so that we are through with the final outcome before we make a new call. 

![use-promises-whenever-making-any-asynchronous-calls.png](/static/images/blogs/code-review/javascript/use-promises-whenever-making-any-asynchronous-calls.png)